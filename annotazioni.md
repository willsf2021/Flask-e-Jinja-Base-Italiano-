### Cos'è Flask?
- Micro-web framework (Server Side)
- Ha un CORE semplice e altamente *estendibile* e *modulare*.
- Utilizzato per create siti web e *principalmente API's*
- Leggero e perfomante
- Si porta dentro due dependenze:
    - Werkzeug (routing, debugger, WSGI support)
    - Jinja2 (templating)

### Prima di iniziare ricordiamo...
```python
    # Installare Python virtualenv
    > pip install virtualenv

    # Creare l'ambiente virtuale e attivare
    > virtualenv venv
    > venv\scripts\activate

    # Installare le dipendenze a partire del 'requirements.txt', se c'e.
    > pip install -r requirements.txt    

```